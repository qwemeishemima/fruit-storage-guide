# 鲜存记 Codex 开发协作约束

## 项目定位

“鲜存记”是一款面向普通家庭用户的蔬果保存工具。

核心价值：
- 快速查询水果、蔬菜的保存方式。
- 告诉用户能不能放冰箱、怎么放、能放多久。
- 后续逐步加入“我的食材”“保存记录”“临期提醒”等功能。
- 更后期可探索 AI 保存建议，但当前阶段不做 AI。

一句话定位：
鲜存记——查怎么存，记别放坏。

对外描述：
蔬菜水果保存指南，记录食材保鲜期，提醒别放坏。

## 技术栈

- 微信小程序原生开发。
- 使用 WXML / WXSS / JavaScript。
- 当前阶段使用本地静态数据。
- 当前阶段不接云开发。
- 当前阶段不使用 Taro、UniApp、Vue、React 等跨端或前端框架。
- 项目界面语言为中文。

## 当前阶段边界

首版 MVP 只关注：
- 首页信息入口。
- 分类浏览。
- 搜索结果。
- 食材详情展示。
- “我的食材”和“我的”页面骨架或空状态。
- 本地静态食材数据结构。

当前阶段暂不实现：
- 云开发。
- 登录、注册、用户体系。
- 真实消息通知或订阅消息。
- AI 保存建议。
- 复杂的个人食材库存管理。
- 复杂图片资源体系。
- 后台管理系统。

## UI 风格原则

- 清爽、自然、生活化，适合家庭日常工具。
- 信息展示易扫读，优先让用户快速得到保存结论。
- 避免花哨、过度装饰和复杂动效。
- 颜色建议偏自然、干净、温和，例如绿色、米白、浅灰等，但不要让界面只剩单一色系。
- 页面布局优先简洁稳定，适合小程序手机端阅读。
- 食材详情页应突出“一句话结论”“能不能冷藏”“保存方式”“保存时间”。
- 按钮、标签、卡片文案保持短句中文，避免解释性长文堆叠。

## 数据组织原则

- 所有食材数据必须集中管理，不要散落硬编码在页面里。
- 推荐统一放在 `data/foods.js`。
- 页面通过工具函数或统一数据模块读取食材数据。
- 搜索、分类、标签筛选都应基于同一份数据源。
- 首版只需要少量示例数据，但字段结构要能支持后续扩展。

建议食材字段：
- `id`：唯一标识。
- `name`：食材名称。
- `aliases`：别名，例如番茄、西红柿。
- `keywords`：搜索关键词。
- `category`：分类。
- `image`：图片路径或占位值。
- `summary`：一句话保存结论。
- `fridgeAdvice`：是否建议冷藏及原因。
- `storageMethod`：最佳保存方式。
- `roomTempShelfLife`：常温保存时间。
- `coldShelfLife`：冷藏保存时间。
- `sealedAdvice`：是否需要密封。
- `waterAdvice`：是否怕水、是否需要保持干燥。
- `ethyleneAdvice`：是否受乙烯影响。
- `tips`：保存小贴士数组。
- `afterPurchaseAdvice`：买回后处理建议。
- `tags`：标签，例如不能放冰箱、必须冷藏、容易坏、水果、蔬菜。

## 开发协作规则

- 每次开发前先说明准备修改哪些文件以及原因。
- 一次只做一个模块，避免一口气实现整个项目。
- 优先选择最简单、最稳、最适合 MVP 的方案。
- 优先遵循微信小程序原生目录和写法。
- 不提前实现用户没有要求的复杂功能。
- 不为了“完整”而堆功能。
- 不在多个页面重复维护同一份食材数据。
- 修改已有文件前先理解当前结构和已有代码，不随意重构。
- 保持代码结构清晰，方便后续继续迭代。
- 新增公共逻辑时，应放在合适的 `utils` 或数据模块中。
- 页面文案默认使用中文。

## 禁止事项

- 禁止引入 Taro、UniApp、Vue、React 等跨端或前端框架。
- 禁止擅自接入云开发。
- 禁止擅自加入登录、支付、订阅消息、推送、AI 接口等能力。
- 禁止把食材数据硬编码到多个页面文件中。
- 禁止一次性堆完所有远期功能。
- 禁止加入与 MVP 无关的大型依赖或复杂架构。
- 禁止在没有用户要求时重写项目结构或做大规模重构。

## 每次任务完成后的输出要求

完成后需要说明：
- 修改了哪些文件。
- 每个文件改了什么。
- 如何在微信开发者工具中验证。
- 后续还需要做什么。

如果没有写业务代码，也要明确说明本次只完成了哪些规划、配置或文档工作。
