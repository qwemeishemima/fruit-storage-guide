# 鲜存记 Codex 开发协作约束

## 项目定位

“鲜存记”是一款面向普通家庭用户的蔬果保存工具。

核心价值：
- 快速查询水果、蔬菜的保存方式。
- 告诉用户能不能放冰箱、怎么放、能放多久。
- 记录家里的食材保鲜期，帮助用户优先处理快要不新鲜的食材。
- 后续可逐步探索提醒、分享、数据质量增强等轻量能力。
- 更后期可探索 AI 保存建议，但当前阶段不做 AI。

一句话定位：
鲜存记——查怎么存，记别放坏。

对外描述：
蔬菜水果保存指南，记录食材保鲜期，提醒别放坏。

## 技术栈

- 微信小程序原生开发。
- 使用 WXML / WXSS / JavaScript。
- 当前阶段使用本地静态数据。
- 当前阶段不接云开发。
- 当前阶段不使用 Taro、UniApp、Vue、React 等跨端或前端框架。
- 项目界面语言为中文。

## 当前阶段边界

项目已完成本地 MVP，当前进入“本地 MVP 收尾阶段”。

当前代码已经实现：
- 首页搜索入口、分类入口、快捷专题入口、常查食材和今日小贴士。
- 分类浏览、搜索结果、标签筛选结果。
- 食材详情展示和食材不存在状态。
- 从详情页加入“我的食材”。
- 添加“我的食材”时支持“今天买的 / 昨天买的 / 前几天买的”快捷选择。
- “我的食材”使用本地 storage 保存记录，storage key 为 `myFoods`。
- “我的食材”记录删除、清空、状态计算、按状态排序和概览。
- “我的”页本地数据概览。
- 本地静态食材数据结构和基础查询工具。

本地 MVP 收尾优先关注：
- 我的食材按状态分组展示。
- 编辑购买/放入日期。
- 首页轻量临期提醒入口。
- 食材备注。
- 分享详情页。
- 数据质量继续校对。
- 搜索别名和标签体验优化。

当前阶段暂不实现：
- 云开发。
- 登录、注册、用户体系。
- 支付、会员。
- 真实消息通知或订阅消息。
- AI 保存建议或 AI 接口。
- 复杂的个人食材库存管理。
- 复杂图片资源体系。
- 后台管理系统。
- 云端同步或多设备同步。

## UI 风格原则

- 清爽、自然、生活化，适合家庭日常工具。
- 信息展示易扫读，优先让用户快速得到保存结论。
- 避免花哨、过度装饰和复杂动效。
- 颜色建议偏自然、干净、温和，例如绿色、米白、浅灰等，但不要让界面只剩单一色系。
- 页面布局优先简洁稳定，适合小程序手机端阅读。
- 食材详情页应突出“一句话结论”“能不能冷藏”“保存方式”“保存时间”。
- “我的食材”页应突出状态、剩余时间感和下一步动作。
- 按钮、标签、卡片文案保持短句中文，避免解释性长文堆叠。

## 数据组织原则

- 所有食材数据必须集中管理，不要散落硬编码在页面里。
- 食材数据统一放在 `data/foods.js`，当前约 142 条。
- 分类数据统一放在 `data/categories.js`，当前约 8 类。
- 食材查询、搜索、分类、标签筛选优先使用 `utils/food.js`。
- “我的食材”本地存储、状态计算、排序等逻辑集中在 `utils/pantry.js`。
- 页面通过工具函数或统一数据模块读取食材数据。
- 搜索、分类、标签筛选都应基于同一份数据源。
- 新增字段或调整结构时，要优先考虑已有页面和本地 storage 的兼容性。

建议食材字段：
- `id`：唯一标识。
- `name`：食材名称。
- `aliases`：别名，例如番茄、西红柿。
- `keywords`：搜索关键词。
- `category`：分类。
- `image`：图片路径或占位值。
- `summary`：一句话保存结论。
- `fridgeAdvice`：是否建议冷藏及原因。
- `storageMethod`：最佳保存方式。
- `roomTempShelfLife`：常温保存时间。
- `coldShelfLife`：冷藏保存时间。
- `shelfLife`：用于本地状态计算的结构化保鲜期信息。
- `sealedAdvice`：是否需要密封。
- `waterAdvice`：是否怕水、是否需要保持干燥。
- `ethyleneAdvice`：是否受乙烯影响。
- `tips`：保存小贴士数组。
- `afterPurchaseAdvice`：买回后处理建议。
- `tags`：标签，例如不能放冰箱、必须冷藏、容易坏、水果、蔬菜。

## 开发协作规则

- 每次开发前先说明准备修改哪些文件以及原因。
- 一次只做一个模块，避免一口气实现整个项目。
- 优先选择最简单、最稳、最适合当前阶段的方案。
- 优先遵循微信小程序原生目录和写法。
- 不提前实现用户没有要求的复杂功能。
- 不为了“完整”而堆功能。
- 不在多个页面重复维护同一份食材数据。
- 修改已有文件前先理解当前结构和已有代码，不随意重构。
- 继续小步迭代，避免大规模重构。
- 保持代码结构清晰，方便后续继续迭代。
- 新增公共逻辑时，应放在合适的 `utils` 或数据模块中。
- 页面文案默认使用中文。

## 禁止事项

- 禁止引入 Taro、UniApp、Vue、React 等跨端或前端框架。
- 禁止擅自接入云开发。
- 禁止擅自加入登录、注册、用户体系。
- 禁止擅自加入支付、会员。
- 禁止擅自加入订阅消息、推送、真实提醒。
- 禁止擅自加入 AI 接口或 AI 保存建议。
- 禁止擅自加入后台管理系统。
- 禁止把食材数据硬编码到多个页面文件中。
- 禁止一次性堆完所有远期功能。
- 禁止加入与本地 MVP 收尾无关的大型依赖或复杂架构。
- 禁止在没有用户要求时重写项目结构或做大规模重构。

## 每次任务完成后的输出要求

完成后需要说明：
- 修改了哪些文件。
- 每个文件改了什么。
- 如何在微信开发者工具中验证。
- 后续还需要做什么。

如果没有写业务代码，也要明确说明本次只完成了哪些规划、配置或文档工作。
